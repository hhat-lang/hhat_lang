program = exprs* EOF
exprs = expr (':' expr)*
expr = ( '(' exprs+ ')' ) / operation / array / literal
operation = ( id ( '(' (expr / exprs)+ ')' )? )
array = '[' expr* ']'
id = r"(\!)?(\`)?(\@)?[a-zA-Z\-\+][a-zA-Z\-\+_0-9]*"
literal = BOOL / INT
BOOL = r"T|F"
INT = r"(0|-?[1-9][0-9]*)"
